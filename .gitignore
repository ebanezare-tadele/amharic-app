# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Output of scripts/generate-official-audio.mjs (run locally or via the
# generate-audio.yml Actions workflow) — never committed.
scripts/official-audio-out/
scripts/amharic-audio.zip
scripts/__pycache__/

# Output of scripts/export-verification-texts.mjs and the
# verify_audio_content*.py scripts (run locally or via the
# verify-audio-content-*.yml Actions workflows, which regenerate these
# fresh every run) — derived from content.mjs/the shipped manifest, never
# committed.
scripts/verification-texts.json
scripts/verification-report*.json
